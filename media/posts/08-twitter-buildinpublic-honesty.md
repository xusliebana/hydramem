# X/Twitter — Build-in-public (honestidad)

```
Most "AI memory" demos hide the failure cases.

So I shipped the opposite: HydraMem's Night Gardener emits ZERO relations when there's
no real evidence, and `hydramem stats --raw` lets you audit every claimed token saving.

Anti-hype as a feature. Local, MIT: <repo>
#buildinpublic #LocalLLaMA
```
