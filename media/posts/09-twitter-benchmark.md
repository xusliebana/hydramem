# X/Twitter — Benchmark (honesto)

```
Quick honest benchmark from HydraMem (offline, stub embedder so it's reproducible):

vector-only → MRR 0.16
full hybrid (vector + graph + BM25) → MRR 0.52

3x on a keyword-aligned set. It's a SANITY check, not SOTA — the public
MuSiQue/LongMemEval run is next. I won't claim numbers I haven't measured.

Local, MIT: <repo>
[ADJUNTA PNG DE LA TABLA]
```
