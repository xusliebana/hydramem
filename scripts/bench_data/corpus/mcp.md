# MCP Server

HydraMem exposes its capabilities to AI clients through a Model Context Protocol
server built on FastMCP. The server offers tools for retrieval, ingestion,
verification, and graph curation over either stdio or streaming HTTP transport.
Every tool call is logged to local telemetry for auditing.
